export var add = (x,y) =>{
  return x+y;
}
export var sumTotal = (subTotal, total) =>{
  return `$${add(subTotal,total)}`
}
export var minus = (x,y) =>{
  return x-y;
}

export var divide = (x,y) => {
  return x/y;
}

export var multiply = (x,y) => {
  return x*y;
}
